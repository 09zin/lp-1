#array mais de 2D
code that test how many dimensions a array can have (theres no limit)

#tamanho array
code that mesures the maximun size for a array

#tamanho arrayaux
a aux for the last to show what cound't show in that code

#tempo array
a code that shows the difference of puts the 2 dimension bigger then the 1
incresing the perfomance