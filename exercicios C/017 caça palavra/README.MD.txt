## crossword
1° version of the code has only 3 words, works but the other works better

##crosswordbetter
2° and true version of the code, improvements are done like use of loops to write
things, and constants to modify it easier, its a code that has 7 words to the player guess
can shows tips on the screen or not, your progress remains even if you miss the word you
hit earlier, works by having a string 2D for display when the player writes a letter/word
right it overwirte the display string (that is the string that is always showed), if all the letters
are the same the hit counts is incremented, when the count hits the number or words (or the player quit)
the code stops